[Unit]
Description={{ app_name }} daemon
After=network.target

[Service]
Type=simple
User={{ USER }}
Group={{ USER }}
Restart=always

ExecStart={{ PATH_TO_PYTHON }} {{ PATH_TO_BOT_SCRIPT_DEST }}/{{ LIVE_APP_SCRIPT }}

[Install]
WantedBy=multi-user.target

# vim: ai et ts=4 sw=4 sts=4 nu
